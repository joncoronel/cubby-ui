---
title: Textarea
description: Multi-line text input for longer form content
type: primitive
---

import { Textarea } from "@/registry/default/textarea/textarea";

A textarea collects multi-line text that grows with its content. Use it for comments, messages, or descriptions; for a single line such as a name or email, use [`Input`](/docs/components/input) instead.

## Preview

<ComponentPreview component="textarea" example="textarea-basic" />

## Installation

<ComponentInstall component="textarea" />

## Usage

<ComponentUsage component="textarea" />

## Examples

### With Label

Pair the textarea with a `Label` for an accessible, clickable field name.

<ComponentPreview component="textarea" example="textarea-with-label" />

### With Helper Text

Add helper text below the field for guidance or validation messages.

<ComponentPreview component="textarea" example="textarea-with-helper-text" />

### Controlled

Use `value` and `onChange` to drive the textarea's value yourself.

<ComponentPreview component="textarea" example="textarea-controlled" />

### Disabled State

Set `disabled` to block input and dim the field.

<ComponentPreview component="textarea" example="textarea-disabled-state" />

### Elevated Variant

Use `variant="elevated"` for a translucent overlay that adapts to the substrate, so the field reads clearly on a Card, Dialog, or popover instead of collapsing into it. See the [Surfaces](/docs/getting-started/surfaces) docs.

<ComponentPreview component="textarea" example="textarea-elevated" />

| Variant    | Description                                                  |
| ---------- | ----------------------------------------------------------- |
| `default`  | Opaque `bg-input`, for fields on the page background.       |
| `elevated` | Translucent `bg-input-elevated`, for raised surfaces.       |

## API Reference

### Props

<ApiPropsList>

<ApiProp
  name="variant"
  fullType='"default" | "elevated"'
  simpleType="string"
  defaultValue='"default"'
>
  Surface appearance. `default` uses opaque `bg-input`. `elevated` uses translucent `bg-input-elevated` for use inside Card, Dialog, or popover surfaces. See the [Surfaces](/docs/getting-started/surfaces) docs.
</ApiProp>

</ApiPropsList>
