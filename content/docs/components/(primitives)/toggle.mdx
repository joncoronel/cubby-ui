---
title: Toggle
description: Two-state button for toggling between on and off
type: primitive
---

import { Toggle } from "@/registry/default/toggle/toggle";

A toggle is a two-state button that stays pressed when on. Use it for a single on/off control like bold, mute, or pin. For a set of mutually exclusive or multi-select toggles, use [`Toggle Group`](/docs/components/toggle-group); for a labeled settings switch, use [`Switch`](/docs/components/switch).

## Preview

<ComponentPreview component="toggle" example="toggle-basic" />

## Installation

<ComponentInstall component="toggle" />

## Usage

<ComponentUsage component="toggle" />

## Examples

### Sizes

Use `size` to set the toggle's height and padding.

<ComponentPreview component="toggle" example="toggle-different-sizes" />

| Size      | Description                       |
| --------- | --------------------------------- |
| `sm`      | Compact, for dense toolbars.      |
| `default` | The standard height.              |
| `lg`      | Taller, for prominent controls.   |

### With Text

Place a text label alongside the icon inside the toggle.

<ComponentPreview component="toggle" example="toggle-with-text" />

### Outline Variant

Set `variant="outline"` to give the toggle a border and subtle shadow.

<ComponentPreview component="toggle" example="toggle-outline-variant" />

| Variant   | Description                              |
| --------- | ---------------------------------------- |
| `default` | Borderless, fills only when pressed.     |
| `outline` | Bordered with a subtle shadow.           |

### Controlled

Use `pressed` and `onPressedChange` to drive the toggle state yourself.

<ComponentPreview component="toggle" example="toggle-controlled" />

### Disabled State

Use `disabled` to prevent interaction.

<ComponentPreview component="toggle" example="toggle-disabled-state" />

## Accessibility

### Label icon-only toggles

An icon-only toggle has no text for a screen reader to announce. Give it an `aria-label` describing the action it controls.

```tsx showLineNumbers
<Toggle aria-label="Toggle bold">
  <BoldIcon />
</Toggle>
```

## API Reference

The Toggle component is built on top of [Base UI's Toggle](https://base-ui.com/react/components/toggle). All Base UI props are supported. The documentation below only covers custom props and modified defaults specific to our implementation.

For the complete Base UI API, see the [Base UI Toggle documentation](https://base-ui.com/react/components/toggle).

### Custom Props

<ApiPropsList>

<ApiProp
  name="variant"
  fullType='"default" | "outline"'
  simpleType="string"
  defaultValue='"default"'
>
  Visual style variant for the toggle button. Custom prop not available in Base UI.
</ApiProp>

<ApiProp
  name="size"
  fullType='"default" | "sm" | "lg"'
  simpleType="string"
  defaultValue='"default"'
>
  Size variant for the toggle button. Custom prop not available in Base UI.
</ApiProp>

</ApiPropsList>
